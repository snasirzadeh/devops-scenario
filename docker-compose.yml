services:
  nginx:
    build: .
    networks:
      - isolated
    ports:
      - "80:8080"
    volumes:
      - ./html:/usr/share/nginx/html
      - ./nginx:/etc/nginx/nginx.conf
      - ./logs:/var/log/nginx
networks:
  isolated:
    driver: bridge
