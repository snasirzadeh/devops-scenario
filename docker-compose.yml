services:
  nginx:
    build: .
    networks:
      - isolated
    ports:
      - "80:8080"
    volumes:
      - ./html:/usr/share/nginx/html
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./logs:/var/log/nginx
networks:
  isolated:
    driver: bridge
