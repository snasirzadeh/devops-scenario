/home/debian/devops-scenario/logs/*.log {
    every 3 days
    missingok
    rotate 10
    compress
    delaycompress
    notifempty
    sharedscripts
    postrotate
        docker exec nginx nginx -s reopen >/dev/null 2>&1 || true
    endscript
}
